--- a/src/services/petsService.js
+++ b/src/services/petsService.js
@@
 export function add(pet) {
-  if (!pet?.name || !pet?.tag) throw new Error("Invalid pet");
+  // Validation centralisée côté service
+  if (!pet || typeof pet !== "object") throw new Error("Invalid pet");
+  if (!pet.name || !pet.tag) throw new Error("Invalid pet");
   const id = Math.max(0, ...db.pets.map((p) => p.id)) + 1;
   const newPet = { id, name: pet.name, tag: pet.tag };
   db.pets.push(newPet);
   return newPet;
 }
