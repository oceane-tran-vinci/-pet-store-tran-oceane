--- a/src/routes/pets.js
+++ b/src/routes/pets.js
@@
-router.get("/", (req, res) => {
-  const limit = Number(req.query.limit ?? 100);
-  const data = pets.list(limit);
+router.get("/", (req, res) => {
+  const limit = Number(req.query.limit ?? 100);
+  const tag = req.query.tag;
+  let data = pets.list(limit);
+  if (tag) {
+    data = data.filter((p) => p.tag === tag);
+  }
   res.json(data);
 });
